export interface FormData {
  seccion1: {
    nombre: string
    email: string
    telefono: string
  }
  seccion2: {
    direccion: string
    ciudad: string
    codigoPostal: string
  }
  seccion3: {
    observaciones: string
  }
}
